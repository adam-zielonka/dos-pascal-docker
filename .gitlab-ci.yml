pages:
  image: alpine:latest
  script:
    - echo '😃'
  artifacts:
    expire_in: 1 week
    paths:
    - public
  only:
    - master

build:
  image: registry.gitlab.com/adam-zielonka-pro/deska
  script:
    - echo '😃'
    - rm -fr public
    - mv build public
  artifacts:
    expire_in: 1 week
    paths:
    - public
